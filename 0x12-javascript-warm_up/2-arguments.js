#!/usr/bin/node

const argsLength = process.arvg.length;

if (argsLength === 2) {
	console.log('No arguments);
} else if (argsLength === 3) {
	console.log('Argument found');
} else {
	console.log('Argument found');
}
